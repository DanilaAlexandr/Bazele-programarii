program test7;

var
  v1, v2, v, s, t: real;

begin
  write('Valentina do Marina=');
  readln(v1);
  write('Marina do Valentina=');
  readln(v2);
  write('puti=');
  readln(s);
  v := v1 + v2;
  writeln(v);
  t := s / v;
  writeln(t);
end.
